import SwiftUI

@available(iOS 13.0, *)
public struct DefaultHorizontalValueView: View {
    public var body: some View {
        Capsule()
            .foregroundColor(.accentColor)
            .frame(height: 3)
    }
}
