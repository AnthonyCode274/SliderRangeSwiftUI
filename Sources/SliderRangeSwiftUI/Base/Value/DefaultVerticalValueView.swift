import SwiftUI

@available(iOS 13.0, *)
public struct DefaultVerticalValueView: View {
    public var body: some View {
        Capsule()
            .foregroundColor(.accentColor)
            .frame(width: 3)
    }
}

