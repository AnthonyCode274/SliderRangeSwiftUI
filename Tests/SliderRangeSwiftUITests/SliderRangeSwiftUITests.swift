import XCTest
@testable import SliderRangeSwiftUI

final class SliderRangeSwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SliderRangeSwiftUI().text, "Hello, World!")
    }
}
